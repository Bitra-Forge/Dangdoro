{
  "permissions": {
    "allow": [
      "Bash(npm run *)"
    ]
  },
  "tools": [
    {
      "name": "firebase",
      "description": "Firebase CLI tool",
      "command": "firebase",
      "args": [
        "--json"
      ]
    }
  ]
}